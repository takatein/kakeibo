# 家計簿AI - 残タスク一覧

最終更新: 2026-02-23

---

## 1. フロントエンド → API 接続（localStorage モック → 本番 API）

**優先度: CRITICAL（MVP必須）**

現在すべてのAPIが localStorage モックで動いている。Lambda は実装済みなので、フロント側の切替が必要。

### 1-1. transactions.ts（7箇所）

| 関数 | 行 | 現状 | 変更内容 |
|------|-----|------|----------|
| `submitInput()` | L30-51 | `mockCategorize()` | → `apiClient.post('/input', req)` |
| `confirmInput()` | L55-84 | localStorage 保存 | → `apiClient.post('/input/confirm', req)` |
| `updateTransaction()` | L112 | localStorage 更新 | → `apiClient.put('/transactions/{txnId}', updates)` |
| `deleteTransaction()` | L128 | localStorage 削除 | → `apiClient.delete('/transactions/{txnId}')` |
| `getTransactions()` | L135 | localStorage 読取 | → `apiClient.get('/transactions', { month })` |
| `getMonthlySummary()` | L146 | localStorage 集計 | → `apiClient.get('/summary', { month })` |
| `FAMILY_ID` | L12 | `'demo-family'` ハードコード | → Cognito `custom:familyId` から取得 |

### 1-2. family-settings.ts（2箇所）

| 関数 | 行 | 変更内容 |
|------|-----|----------|
| `getFamilySettings()` | L33 | → `apiClient.get('/knowledge/settings')` |
| `updateFamilySettings()` | L42 | → `apiClient.put('/knowledge/settings', updates)` |

### 1-3. fixed-costs.ts（4箇所）

| 関数 | 行 | 変更内容 |
|------|-----|----------|
| `getFixedCosts()` | L23 | → `apiClient.get('/knowledge/fixed-costs')` |
| `addFixedCost()` | L31 | → `apiClient.post('/knowledge/fixed-costs', cost)` |
| `updateFixedCost()` | L54 | → `apiClient.put('/knowledge/fixed-costs/{costId}', updates)` |
| `deleteFixedCost()` | L70 | → `apiClient.delete('/knowledge/fixed-costs/{costId}')` |

### 1-4. モック/本番切替の仕組み

- `VITE_USE_MOCK` 環境変数でトグルする仕組みを追加
- 本番: API Gateway 経由で Lambda を呼ぶ
- 開発: 現行の localStorage モックをフォールバックで残す

---

## 2. Cognito 認証の実装

**優先度: CRITICAL（MVP必須）**

UI（LoginScreen）とルーティング（ProtectedRoutes）は実装済み。AuthContext 内のログイン/サインアップが mock。

### やること

| タスク | ファイル | 詳細 |
|--------|----------|------|
| Cognito signIn 実装 | `src/contexts/AuthContext.tsx` L39-52 | `amazon-cognito-identity-js` の `authenticateUser()` に差し替え |
| Cognito signUp 実装 | `src/contexts/AuthContext.tsx` L54-67 | `CognitoUserPool.signUp()` + メール認証フロー |
| セッション復元 | `src/contexts/AuthContext.tsx` L28-36 | Cognito セッションからトークン復元 |
| JWT トークン取得 | `src/api/client.ts` L21 | localStorage の `demo-token` → Cognito ID トークン |
| パスワードリセット | LoginScreen | 「パスワードを忘れた」フロー追加（Nice-to-have） |

### 依存関係

- `amazon-cognito-identity-js` はインストール済み（package.json）
- `src/api/config.ts` に Cognito 設定のひな型あり
- 環境変数 `VITE_COGNITO_USER_POOL_ID`, `VITE_COGNITO_CLIENT_ID` が必要

---

## 3. CI/CD パイプライン修正

**優先度: HIGH**

### 3-1. GitHub Pages デプロイ（現在の構成）

- [x] `cache: 'npm'` の削除
- [x] `npm ci` → `npm install` に変更
- [x] GitHub Pages 向けワークフローに書き換え
- [ ] GitHub リポジトリ Settings → Pages → Source を "GitHub Actions" に設定
- [ ] **推奨**: ローカルで `npm install` 実行して `package-lock.json` をコミット → `npm ci` に戻す

### 3-2. AWS デプロイ（本番移行時）

GitHub Secrets の設定が必要:

| Secret 名 | 説明 | 必須 |
|-----------|------|------|
| `AWS_ROLE_ARN` | GitHub OIDC 用 IAM ロール ARN | 必須 |
| `FRONTEND_BUCKET` | S3 バケット名（CDK スキップ時のフォールバック） | 任意 |
| `CLOUDFRONT_DISTRIBUTION_ID` | CF ディストリビューション ID | 任意 |

OIDC 初回セットアップ:

```bash
# 1. OIDC ロール作成
aws cloudformation deploy \
  --stack-name kakeibo-github-oidc \
  --template-file infra/github-oidc-role.yml \
  --capabilities CAPABILITY_NAMED_IAM \
  --parameter-overrides GitHubOrg=<your-github-username> GitHubRepo=kakeibo

# 2. Role ARN を取得 → GitHub Secrets に設定
aws cloudformation describe-stacks \
  --stack-name kakeibo-github-oidc \
  --query "Stacks[0].Outputs[?OutputKey=='RoleArn'].OutputValue" \
  --output text

# 3. CDK ブートストラップ
cd infra && npm install && npx cdk bootstrap
```

---

## 4. 環境変数の設定

**優先度: HIGH**

`.env.example` をコピーして `.env` を作成し、CDKデプロイ後の実値を入れる。

```bash
cp .env.example .env
```

| 変数 | 取得元 |
|------|--------|
| `VITE_AWS_REGION` | `ap-northeast-1`（固定） |
| `VITE_COGNITO_USER_POOL_ID` | CDK outputs or AWS Console |
| `VITE_COGNITO_CLIENT_ID` | CDK outputs or AWS Console |
| `VITE_API_BASE_URL` | CDK outputs (`ApiUrl`) |
| `VITE_S3_BUCKET` | CDK outputs (`ReceiptBucketName`) |

---

## 5. レシート解析 Lambda

**優先度: MEDIUM（Phase 2）**

仕様書 `backend/agents/receipt-parser.md` は完成済み。Lambda 実装がない。

### やること

- [ ] `infra/lambda/receipt-parser/index.mjs` を作成
  - Amazon Nova Pro（Bedrock）でマルチモーダル推論
  - OCR + AI で店名・品目・金額を抽出
  - レスポンスを ConfirmItem[] 形式に変換
- [ ] CDK スタックに `POST /receipt/parse` ルートを追加
- [ ] フロントの `InputPage` からレシート撮影 → アップロード → 解析フローを接続

---

## 6. AI カテゴリ分類のルート追加

**優先度: LOW**

`ai-categorize` Lambda は実装済みだが、API Gateway のルートが未設定。

- [ ] CDK スタックに `POST /ai-categorize` ルートを追加

---

## 7. 重複 Lambda の整理

**優先度: LOW**

- `infra/lambda/fixed-costs/index.mjs` と `infra/lambda/knowledge/index.mjs` の固定費処理が重複
- knowledge Lambda 側に統合し、fixed-costs Lambda は削除を検討

---

## 8. セキュリティ（本番リリース前）

**優先度: HIGH**

| 項目 | 深刻度 | 対象ファイル | 詳細 |
|------|--------|-------------|------|
| S3 CORS 制限 | HIGH | `infra/lib/kakeibo-stack.ts:74-80` | `allowedOrigins: ['*']` → CloudFront ドメインに絞る |
| API Gateway CORS 制限 | HIGH | `infra/lib/kakeibo-stack.ts:274-278` | `ALL_ORIGINS` → 自ドメインのみ |
| 金額入力の上限バリデーション | MEDIUM | `ConfirmDialog.tsx`, `EditTransactionModal.tsx` | 現在 `> 0` のみ。上限チェック追加 |
| レシートアップロードのサーバーサイドバリデーション | MEDIUM | Lambda 側 | MIME チェックが Client 側のみ |
| CloudFront セキュリティヘッダー | MEDIUM | infra | CSP, X-Frame-Options, HSTS 等を追加 |
| Cognito パスワードポリシー | LOW | `kakeibo-stack.ts:134-140` | `requireUppercase: true`, `requireSymbols: true` |
| API レート制限 | LOW | API Gateway | WAF or Usage Plan で制限追加 |

### 対応済み

- [x] `JSON.parse` の try-catch 安全化 (`safeGetJson` ユーティリティ導入)
- [x] TypeScript ビルドエラー全修正

---

## 進捗サマリー

| カテゴリ | 完了 | 残り |
|----------|------|------|
| Lambda 関数 | 8/8 | ルート追加 2件 |
| CDK インフラ定義 | 95% | ルート追加のみ |
| CI/CD パイプライン | 90% | Secrets 設定 + OIDC セットアップ |
| フロント API 接続 | 1/14 | 13 関数の差し替え |
| Cognito 認証 | UI のみ | 実ログイン処理 |
| レシート解析 | 仕様のみ | Lambda 実装 |

**推定完了度: 85% → MVP まであと 15%**

### 推奨実装順序

```
1. Cognito 認証実装     ← マルチユーザーの基盤
2. フロント API 接続     ← データ永続化
3. 環境変数 + デプロイ   ← 動作確認
4. レシート解析          ← Phase 2
```
